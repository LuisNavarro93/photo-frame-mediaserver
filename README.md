# photo-frame-mediaserver
Scripts and config for a dual-purpose home appliance: an unattended portrait-orientation photo slideshow that powers itself on and off daily via an RTC wake alarm, plus a Jellyfin media server for streaming to the home network. Includes power scheduling, boot/shutdown logging, slideshow autostart, and SSH/FTP access setup.
